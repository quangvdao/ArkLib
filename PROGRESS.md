# Active continuation: reconciled ownership and integrated proof chains

The user selected this task as the continuation owner. The ownership-pending text below is
preserved historical context, superseded by `ACTIVE-CONTINUATION.md` in the reconciliation
archive. Existing unfinished work from the disconnected executor and its workers has been
snapshotted and reconciled additively. Six legacy deletion proposals remain held.

The integration tree now contains checked automatic first-order parameters and an actual
strict-support symbolic certificate, all-characteristic Frobenius factor extraction, distinct
ordinary factors, actual graph recognition/counting and full-agreement retained families,
regular ordinary finite exceptional sets, and a polynomial-valued resultant bridge with exact
`(2*b-1)*h` exceptions. The actual-Y1-degree hybrid descent is checked. The general uniform
capacity envelope and executable decoder chain also compile. Twelve new rate-partition module
relocations and the five-declaration executable-eligibility split have reviewed narrow ledgers.

The current checkpoint passes both `./scripts/validate.sh` and `./scripts/validate.sh --axioms`,
including all maintained examples, acceptance tests, runtime checks, source policy, import
boundaries, and the unchanged axiom baseline (no new taint). Integration fixes preserve the old
ambient-search cost interval, distinguish the two list-certificate characteristic interfaces,
and express the jet-budget test as a kernel-checked example. Logs and independent reviews are
recorded in the reconciliation archive. Public ordinary/Johnson and hybrid capstones, final
caller migration, appendix, and exact-final-tree validation remain pending. Three bounded Sol
High workers are advancing ordinary root transport, the actual Johnson interpolant, and the
remaining hybrid tail proofs. This is a verified implementation checkpoint, not completion.

---

# Revised resume: checked isolated proofs, integration ownership pending

The resumed task 01a082d1… has preserved the original staged/unstaged state and completed
isolated Frobenius/ordinary supporting proofs plus the Theorem 1.2 optimized/closed constant
arithmetic. Johnson finite bounds (including strict 8/3 and 16/49 comparisons and the printed
denominator bridge) pass targeted validation and independent review. The source/log archive is
pinned. The public ordinary and hybrid MCA capstones remain incomplete.

Evidence, exact source hashes, independent reviews, preserved patches, and the precise
remaining mathematical obligations are in:
`/Users/quangdao/Documents/Lean/rs-rate-first-handoff-20260908/resume-reconciliation-2109/`.
See `supporting-source-manifest.json`, `SUPPORTING-REVIEW.md`, and `REMAINING.md`.

The still-active task 01a0821f… owns overlapping work in ArkLib-rate-first-personal2-20260908.
Ownership messages remain queued; the user has been asked to choose one integration owner.
No source integration, full validation, commit, push, deletion, or manuscript edit has occurred
in this resume task. The original integration index is unchanged, and the earlier progress
history remains below. The deletion allow-list remains empty.

---

# Rate-first implementation progress

## Revised execution authorized — ownership reconciliation in progress

On 2026-09-08 the user explicitly resumed the revised `CONTINUE-PROMPT.md` in
Personal 2 task `01a082d1-a3ae-7f73-b994-c6bdde9fb77f`. The historical pause
below is no longer a missing-user-authorization blocker. The revised Johnson /
Frobenius-first order and the optimized plus closed Theorem 1.2 constants apply.

Reconciliation found this checkout still at `c81bd94e29ea722aa1c0415a5072206830393642`,
with 23 dirty files, including additions newer than the plan audit. All were
preserved with separate staged/unstaged patches and SHA-256 manifest under
`../rs-rate-first-handoff-20260908/resume-reconciliation-2109/`. No existing staged
or unstaged content was discarded. The source proofs have not been independently
revalidated in this resume task.

A separate live executor (`01a0821f-9849-7ec0-ac2b-b4e84c32ddb4`) is building
`ArkLib-rate-first-personal2-20260908`. Ownership messages were queued through
Switchboard; acknowledgment remains pending. To obey the no-competing-executor
requirement, this resume task has made no Lean-source integration changes,
commits, pushes, or deletions. Its only implementation assignments are isolated
new Frobenius-pullback and hybrid-constant helper modules, with an independent
Sol High arithmetic review. Their results are not yet integration evidence.

The arithmetic review confirms the closed constants but identifies a required
proof-interface detail: retain raw real cardinality bounds before ceiling.
For n=4,D=1,A=3,mu=2,M=0,h=1, raw EClosed=47/2 whereas the optimized ceiling is 24.
Thus the closed-real capstone cannot follow from a ceiling-only statement.
The review and current ownership/deletion/validation boundaries are recorded
beside the snapshot. The deletion allow-list remains empty; full validation and
literal final-commit excerpts remain outstanding.

## Concurrent specification conflict — awaiting owner decision

At 2026-09-08 11:38 MST, the previously read `HANDOFF.md` was found replaced by a
revision dated 11:36:53. Its opening now says “plan only; execution is paused” and
changes the dependency order to Johnson/order-zero Frobenius MCA before first
order. This contradicts this thread's explicit original execution request and
the original frozen rate-first launch specification. Parent requested all three
workers pause and preserve their isolated worktrees, and escalated the scope
decision to the user. No work is discarded; no integration commit or push exists.

Observed revised handoff SHA-256:
`5aa9b4302b4fa02cbd6684c8a8ce5c5466736b7eb1c09fcc980942b45c95f065`.

Additional targeted checks passed before this conflict: `BlockLength`, `Recipe`,
`Symbolic.RateGateCertificate`, constant-curve LD consumption, and finite rate
LD/MCA consumers. Their internal moment hypothesis is not discharged yet.
New-module headers and import wrapping follow the explicit CONTRIBUTING.md:167
header requirement; the Lean source-policy gate now passes. Module paths
`NormalizedDimension` and `RatePartitionCertificate` were shortened to
`MomentSource` and `VariableHeight` respectively. Declaration names are unchanged.
No full `validate.sh --axioms`, independent final review, final handoff or final
paper synchronization patch is claimed.

Baseline: `c81bd94e29ea722aa1c0415a5072206830393642`.
Fetched target `fork/quang/rs-capacity-and-correlated-agreement` on 2026-09-08;
no newer commits. Integration checkout was clean. Mutable manuscript untouched.

## Ownership and phases

- Parent: integration, variable-margin symbolic certificate, generic LD/MCA consumers,
  later public migration, deletion closure, final validation and non-force push.
- Sol High Peirce: private `ArkLib-rate-first-analytic-20260908` worktree;
  first-order threshold, finite rounding, certificate adapter.
- Sol High Carver: private `ArkLib-rate-first-moment-20260908` worktree;
  ordered simplex and exact 27/10 moment.
- Later bounded phases: finite ratio and explicit executable jet budget; general/fixed-rate/
  uniform capstones; independent non-author review.
- Sol High Faraday: private `ArkLib-rate-first-budget-20260908` worktree;
  explicit strict natural jet budget `J`, matrix/search/decoder and primitive-work proofs.

Dependency and project outputs are private APFS copies, not shared writable build directories.
No baseline relaxation, new sorry, extra axioms, or native trust is authorized.

## Required coverage ledger

| Paper content | Owner / intended declaration layer | Status |
|---|---|---|
| First-order root and clean surplus | Parameters/FirstOrder/RateBound | In progress |
| First-order finite recipe | Parameters/FirstOrder/RateRounding | In progress |
| First-order LD then line MCA | Public Capacity/FirstOrderRate | Pending |
| Ordered simplex 27/10 moment | Parameters/RatePartition/Moment | In progress |
| Finite ratio and Gamma gate | Parameters/RatePartition/FiniteRatio | Scalar limit/search compiled |
| Generic margin-dependent height | Interpolation/Symbolic | Initial constructor compiles |
| General LD and exact line MCA | Public Capacity/RatePartitionBound | Pending |
| Fixed-R epsilon asymptotic, uniform 3/2 | Parameters rate asymptotics | Pending |
| Explicit integer jet budget and actual run exactness | Executable interpolation/search | In progress |
| Family transfers and certified examples | Existing callers preserved | Pending migration |
| Final source/axiom manifest and literal paper patch | Handoff artifacts | Pending |

## Deletion/caller ledger

No deletions yet. Old uniform 2.7 parameters, fixed 543/500 symbolic constructors,
12-nu heights, qualitative finite-mesh route, and 2*m-only search wrappers remain
until replacement callers compile. Generic support, kernel, Taylor, reconstruction,
filtering, exact agreement, and concrete application certificates must remain.

## Inspected API correction

The inventory's claimed equality of the new support with `WeightedSupportEligible` is
not literal: `fullHigherJetWeight` weights `Y_j` by `j-1`, whereas the new paper
weights it by `j`. The new support must be represented as a finite subspace of the
existing differential-polynomial type, with an actual new local-rank count. This is
an API repair, not evidence against the paper theorem. Do not change the simplex
normalization to match the old support. No deletion or mathematical claim relies
on the mistaken identification.

Targeted check: `lake env lean` on `Interpolation/Symbolic/RatePartitionCertificate.lean`
passes with no output (private dependencies, artifact caches disabled).

## Verified integration-layer additions

The following targeted builds pass without warnings in the added modules:

- `PartitionSupport.Basic`: exact new weights `1,...,d`, finite support, legacy-support inclusion.
- `PartitionSupport.LocalRank`: actual backward-Taylor reachable support and the exact
  ceiling-sum rank upper bound over every field.
- `PartitionSupport.Counting`: exact finite source count equals actual finrank.
- `QuadraticStaircase` and `PartitionSupport.Dimension`: no-loss quadratic source lower bound.
- `PartitionSupport.FloorTransfer`: whole weighted-simplex square integral lower-bounds finrank.
- `Symbolic.PartitionCertificate`: actual finite matrix row bound and primitive symbolic
  certificate with arbitrary jet cap and exact rank-dependent height.
- LD and MCA `Capacity.CertificateBound`: generic exact scalar consumers (targeted Lean checks).
- `RatePartition.Gate`: every-epsilon fixed-rate scalar Gamma gate with the exact ceiling.
- `RatePartition.FiniteRatio`: floor-aware ratio convergence and terminating existence search
  for every strict Gamma margin, including margins below the fixed uniform loss.
- `PartitionSupport.RankBound`: enlarged-simplex lattice bound and exponential rank envelope
  retaining the exact `d+1` contact denominator.
- `PartitionSupport.NormalizedDimension` and `FiniteSurplus`: exact normalization identity
  and actual finite source/rank ratio from the whole-simplex moment. The analytic moment
  is still a hypothesis at this internal layer, not claimed as a public rate theorem.

These are finite/algebraic consumers, not the missing analytic rate capstones. Moment,
finite-ratio limit/loss, public rate/uniform conclusions, final executable instantiations,
deletions, independent review, and final validation/push remain required.
